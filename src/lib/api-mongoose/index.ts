export { TYPES } from './TYPES';

export { Database } from './Database';

export { Document, Model, model, Types, Schema, Query, ConnectOptions } from 'mongoose';

import mongoose from 'mongoose';
export default mongoose;
